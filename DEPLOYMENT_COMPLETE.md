# 🛠️ دليل النشر والتشغيل الكامل
# مكتب رياض القحطاني للسفريات والسياحة والأيدي العاملة

## 📋 المتطلبات الأساسية

### البرامج المطلوبة:
- Node.js 16+ 
- npm أو yarn
- MongoDB (محلي أو Atlas)
- Git

### الخدمات المطلوبة:
- حساب Vercel للنشر
- حساب MongoDB Atlas
- حساب Gmail للإيميلات
- واتساب للاستقبال

## 🚀 خطوات التشغيل

### 1. تحضير المشروع
```bash
# نسخ جميع الملفات
mkdir riyadh-travel-website
cd riyadh-travel-website

# إنشاء الملفات التالية في المجلدات المناسبة
```

### 2. إعداد Backend
```bash
cd backend
npm install

# إنشاء ملف .env
cp .env.example .env

# تحرير ملف .env
nano .env
```

### 3. إعداد قاعدة البيانات
```bash
# تشغيل سكريبت البيانات الأولية
node ../database/seed.js
```

### 4. تشغيل النظام محلياً
```bash
# Backend
cd backend && npm run dev

# Frontend (في terminal آخر)
cd frontend && npm run dev
```

## 🌐 النشر على Vercel

### تحضير Frontend للنشر:
1. رفع المشروع إلى GitHub
2. ربط Vercel بـ GitHub repository
3. إعداد environment variables في Vercel

### Environment Variables في Vercel:
```
NODE_ENV=production
MONGODB_URI=mongodb+srv://...
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password
BUSINESS_EMAIL=info@riyadh-travel.com
BUSINESS_PHONE=+966501234567
JWT_SECRET=your-secret-key
```

### إعداد Backend API:
Backend يجب أن يكون مستضاف منفصلاً على:
- Vercel Serverless Functions
- Heroku
- DigitalOcean
- أو أي خدمة Node.js

## ⚙️ إعدادات متقدمة

### واتساب إعداد:
1. الخادم سيولد QR code عند التشغيل الأول
2. امسح الكود بهاتفك لربط واتساب
3. سيتم حفظ الجلسة تلقائياً

### إيميل إعداد:
1. فعّل 2FA في Gmail
2. أنشئ App Password
3. استخدم App Password في EMAIL_PASS

### قاعدة البيانات:
```bash
# MongoDB Atlas (موصى به)
mongodb+srv://username:password@cluster.mongodb.net/riyadh-travel

# محلي
mongodb://localhost:27017/riyadh-travel
```

## 🔧 استكشاف الأخطاء

### مشاكل شائعة:

#### واتساب لا يتصل:
```
# تحقق من الحالة
GET /api/whatsapp/status

# إعادة تشغيل (في التطوير)
POST /api/whatsapp/restart
```

#### قاعدة البيانات لا تتصل:
```bash
# تحقق من MongoDB URI
echo $MONGODB_URI

# اختبار الاتصال
node -e "require('./models/Service').find().then(() => console.log('DB Connected')).catch(console.error)"
```

#### الإيميلات لا ترسل:
```bash
# تحقق من إعدادات Gmail
# يجب استخدام App Password وليس كلمة المرور العادية
```

## 📊 مراقبة النظام

### API Endpoints للمراقبة:
```bash
# صحة النظام
GET /api/health

# حالة واتساب
GET /api/whatsapp/status

# قاعدة البيانات
GET /api/services (اختبار)
```

### Logs المراقبة:
- Backend logs في console
- Socket.IO events للمراقبة الفورية
- Email delivery status
- WhatsApp message delivery

## 🔒 الأمان

### إعدادات الأمان المطبقة:
- Rate limiting على API
- CORS protection
- Helmet.js security headers
- Input validation
- Sanitized database queries

### توصيات إضافية:
- استخدم HTTPS دائماً
- راجع متغيرات البيئة دورياً
- راقب logs للaktivités غير طبيعية
- احتفظ بنسخ احتياطية من قاعدة البيانات

## 📞 معلومات الدعم

### للتواصل التقني:
- Email: tech@riyadh-travel.com
- Phone: +966501234567
- WhatsApp: +966501234567

### للمراجعة والدعم:
- مراجعة التصميم والألوان
- اختبار جميع الوظائف
- التأكد من ربط واتساب والإيميل
- اختبار عملية الحجز كاملة

## 🎯 الخطوات النهائية

1. ✅ إعداد جميع الملفات
2. ✅ تكوين قاعدة البيانات
3. ✅ ربط واتساب
4. ✅ اختبار الإيميلات
5. ✅ نشر على Vercel
6. ✅ اختبار شامل للنظام
7. ✅ تدريب على الاستخدام

---

**تم إنشاء هذا الدليل بواسطة MiniMax Agent** 🤖
**الموقع جاهز للنشر والاستخدام!** 🚀